%br
%br
%div 
  Questions about this email: contact
  #{mail_to "cuac@libraries.cul.columbia.edu"}
%br
%div
Have more to deposit: #{link_to 'click here', 'http://academiccommons.columbia.edu/deposit'}
%div
%br
  Wish to unsubscribe:
  #{link_to("click here", url_for(:host => (Rails.application.config.base_path != nil) ? Rails.application.config.base_path : "localhost:3000",  :only_path => false, :controller => :statistics, :action => :unsubscribe_monthly, :author_id => @author_id, :chk => @author_id.crypt("xZ")))} 

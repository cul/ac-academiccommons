<%# We use a different log in link during local development %>

<% is_button = local_assigns.fetch(:is_button, false) %>
<% classes = is_button ? 'btn btn-primary' : ''%>

<%= link_to "#{t('blacklight.header_links.login')} #{('(developer)' if Rails.env.development?)}", new_user_session_path, class: classes %>

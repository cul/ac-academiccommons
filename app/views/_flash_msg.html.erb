<div class="flash_messages">
<% [:success, :notice, :error, :alert].each do |type| %>
  <% if flash[type] %>
    <div class="alert <%=type%>"><%= flash[type] %>
       <a class="close" data-dismiss="alert" href="#">&times;</a>
    </div>
  <% end %>
<% end %>
</div>

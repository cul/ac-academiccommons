<% 

if params[:id] == 'departments'
  @page_title = "Browse Departments - #{application_name}"
elsif params[:id] == 'subjects'
  @page_title = "Browse Subjects - #{application_name}"
end

%>

<% sidebar_items << capture do %>
  
  <%#= render :partial=>'facets' %>

<% end %>

<%= render :partial=>params[:id] + "_list" %>

